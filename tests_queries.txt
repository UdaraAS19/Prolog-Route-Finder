% File: tests/test_queries.txt
% Run these commands in the SWI-Prolog CLI to test your algorithms.

?- consult('src/COU4303_Group12.pl').

% 1. Test basic algorithms
?- bfs(a, d, Path).
?- astar(a, d, Path, Cost).

% 2. Test dynamic constraints (blocked roads)[cite: 3]
?- assert(blocked(c, d)).
?- astar(a, d, Path, Cost). % Should find an alternative route

% 3. Compare algorithms[cite: 3]
?- compare_algorithms(a, d).